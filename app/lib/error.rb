# Namespace containing all custom exceptions for the framework application.
#
# Add new error classes under the ./error directory and inherit from either
# Error::ApplicationError or one of its sub-classes.
module Error
end
